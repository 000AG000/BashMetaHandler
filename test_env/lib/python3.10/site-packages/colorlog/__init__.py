"""A logging formatter for colored output."""

import sys
import warnings

from colorlog.formatter import ColoredFormatter
from colorlog.formatter import LevelFormatter
from colorlog.formatter import TTYColoredFormatter
from colorlog.formatter import default_log_colors
from colorlog.wrappers import CRITICAL
from colorlog.wrappers import DEBUG
from colorlog.wrappers import ERROR
from colorlog.wrappers import FATAL
from colorlog.wrappers import INFO
from colorlog.wrappers import NOTSET
from colorlog.wrappers import WARN
from colorlog.wrappers import WARNING
from colorlog.wrappers import StreamHandler
from colorlog.wrappers import basicConfig
from colorlog.wrappers import critical
from colorlog.wrappers import debug
from colorlog.wrappers import error
from colorlog.wrappers import exception
from colorlog.wrappers import getLogger
from colorlog.wrappers import info
from colorlog.wrappers import log
from colorlog.wrappers import root
from colorlog.wrappers import warning


__all__ = (
    "CRITICAL",
    "DEBUG",
    "ERROR",
    "FATAL",
    "INFO",
    "NOTSET",
    "WARN",
    "WARNING",
    "ColoredFormatter",
    "LevelFormatter",
    "StreamHandler",
    "TTYColoredFormatter",
    "basicConfig",
    "critical",
    "debug",
    "default_log_colors",
    "error",
    "exception",
    "exception",
    "getLogger",
    "info",
    "log",
    "root",
    "warning",
)

if sys.version_info < (3, 6):
    warnings.warn(
        "Colorlog requires Python 3.6 or above. Pin 'colorlog<5' to your dependencies "
        "if you require compatibility with older versions of Python. See "
        "https://github.com/borntyping/python-colorlog#status for more information."
    )
